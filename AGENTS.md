# AGENTS.md

## 1. 项目目标

本项目为 **ArriveMate**，一个面向 Robotaxi 后排智能座舱的 AI Agent Web Demo。

ArriveMate 的核心目标不是做一个普通 Chatbot、车控助手或智能客服，而是验证一个新的 Robotaxi 座舱 AI 产品方向：

> 将 Robotaxi 车程转化为“到达前准备空间”，帮助乘客在抵达目的地前完成信息准备、状态调整、行动提醒和座舱环境切换。

Demo 需要体现：

- AI 主动欢迎并理解行程
- AI 主动询问乘客到达后要做什么
- 用户通过预设语音指令模拟输入
- AI 识别用户意图和状态
- AI 生成到达前准备计划
- 用户确认后才执行计划
- 座舱状态随场景联动变化
- 到达前给出收口提醒和 checklist

本项目应优先保证产品概念清晰、交互闭环完整、视觉上符合 Robotaxi 后排大屏，而不是追求真实 API 或复杂功能完整度。

## 2. 技术栈说明与约束

### 2.1 技术栈

本项目使用：

- React
- Vite
- JavaScript
- CSS
- 前端 Mock Data

### 2.2 技术约束

本期 Demo 严格限制为纯前端实现。

不得接入：

- 后端服务
- 数据库
- 用户账号系统
- 真实 ASR/TTS
- 真实地图、路线、ETA、交通 API
- 真实车控 API
- 真实日历、航班、高铁、消息接口
- 真实消息发送
- 真实路线变更或下车点变更

所有场景、行程、ETA、识别结果、AI 文案、座舱状态、异常兜底、通知文案均使用前端本地 mock data。

如需要表现“发送消息”“路线变化”“车控变化”，只能以 mock 状态展示，不得暗示真实执行。

## 3. 产品原则

### 3.1 ArriveMate 不是普通 Chatbot

页面和交互不得以长聊天记录为主。

AI 对话只用于表达当前状态、理解结果、计划说明和到达前提醒。核心信息应通过大卡片和状态面板展示。

### 3.2 ArriveMate 不是普通车控助手

车控不是独立功能集合。灯光、音乐、座椅、温度、提醒方式等座舱变化必须服务于具体到达前准备场景。

例如：

- 面试准备：柔和专注、降低干扰
- 机场准备：ETA 和 checklist 置顶、提醒更明确
- 商务会议：冷静专注、办公坐姿
- 休息恢复：暗光、低打扰、温和唤醒

### 3.3 AI 必须主动引导

默认体验不是“用户打开功能菜单”，而是：

1. 乘客上车
2. AI 主动欢迎
3. AI 确认目的地、ETA、安全状态
4. AI 介绍到达前准备能力
5. AI 主动询问乘客到达后要做什么

### 3.4 AI 不得越权执行

AI 生成计划后，必须等待用户确认。

必须提供三个分支：

- 开启计划
- 调整计划
- 暂不需要

涉及 mock 通知、路线建议、下车点建议等高影响操作时，必须展示二次确认或 mock 说明。

### 3.5 Demo 聚焦 MVP

不得新增 PRD 外的新主场景或新系统能力。

本期只围绕：

- 一套通用主流程
- 四个核心场景
- 一个面试场景中的迟到风险 mock 兜底

## 4. 交互原则

### 4.1 主流程

Demo 主流程必须保持如下顺序：

1. 乘客上车
2. AI 欢迎并确认目的地、ETA、自动驾驶状态、安全状态
3. AI 介绍到达前准备能力
4. AI 主动询问用户到达后要做什么
5. 用户点击预设语音按钮
6. 页面展示用户语音转文字
7. AI 展示意图识别结果
8. AI 生成到达前准备计划
9. 用户选择确认分支
10. 若用户点击“开启计划”，进入座舱联动
11. 到达前 3-5 分钟展示收口提醒

### 4.2 用户输入方式

本期不做真实语音识别。

使用底部 4 个大按钮模拟语音输入：

- 面试准备
- 机场准备
- 商务会议准备
- 休息恢复

点击按钮后，应展示完整语音文本。

不得使用自由文本输入框作为主要交互入口，避免产品看起来像普通 Chatbot。

### 4.3 计划确认分支

AI 生成计划后必须出现：

- 开启计划：主路径，进入座舱联动和到达前提醒
- 调整计划：轻量 mock 分支，只展示二次修改后的计划，不做复杂编辑器
- 暂不需要：进入低打扰待命状态

### 4.4 异常兜底

迟到风险判断只在面试场景中展示。

流程为：

1. 系统 mock 检测到 ETA 延误
2. AI 主动解释原因
3. AI 给出可选方案
4. 用户可生成 mock 通知文案或选择持续监控
5. 涉及发送或变更路线时，必须提示需要二次确认
6. 不做真实发送，不做真实路线变更

必须明确展示：

> 本 Demo 仅展示 mock 提示，不会真实发送消息或变更路线。

## 5. 页面布局原则

页面需要符合 Robotaxi 后排大屏特点：

- 大字号
- 大卡片
- 低信息密度
- 科技感
- 远距离可读
- 尽量减少用户手动操作

页面不应像：

- 普通网页后台
- 手机 App 放大版
- 聊天软件
- 车控设置页
- 功能菜单集合

页面应像：

- Robotaxi 后排智能座舱界面
- AI 到达前准备控制台
- 低触控、强状态、强引导的大屏体验

### 5.1 顶部：行程与安全状态栏

作用：

- 建立 Robotaxi 基础信任感
- 持续展示行程确定性

需要展示：

- 目的地
- 目的地类型
- ETA
- 预计到达时间
- 自动驾驶状态
- 安全状态
- 路线状态
- 当前阶段

ETA 应为最醒目的信息之一。

### 5.2 中部左侧：AI Agent 主交互区

作用：

- 展示 ArriveMate 的主动引导、当前话术、当前状态
- 承载用户最近一次语音转文字

需要展示：

- Agent 名称：ArriveMate
- Agent 当前状态
- AI 当前话术
- 用户语音转文字
- 当前推荐模式
- Agent 状态标签

不要展示长聊天历史。

### 5.3 中部右侧：AI 决策与到达前准备区

作用：

- 展示 AI 的识别、计划、执行进度、异常处理和到达前 checklist

根据流程阶段展示：

- 识别结果卡
- 准备计划卡
- 执行进度卡
- 异常风险卡
- 通知文案 mock 卡
- 到达前 checklist 卡

### 5.4 中下：座舱联动状态区

作用：

- 证明 ArriveMate 不只是聊天，而是能联动座舱环境

需要展示原状态到新状态的变化：

- 灯光
- 音乐
- 座椅
- 温度
- 提醒方式
- 屏幕模式

### 5.5 底部：语音指令模拟区

作用：

- 使用大按钮模拟用户语音输入
- 减少手动操作

需要展示 4 个语音按钮：

- 面试准备
- 机场准备
- 商务会议准备
- 休息恢复

点击后展示完整语音内容。

## 6. 核心场景

### 6.1 面试准备

用户语音：

> 我等会儿要去面试，有点紧张

AI 识别：

- 出行目的：面试
- 用户状态：紧张
- 核心诉求：快速准备 + 稳定状态
- 推荐模式：面试准备模式

AI 计划：

1. 快速回顾公司和岗位
2. 练习 1-2 个常见面试问题
3. 到达前检查简历、证件、作品集和仪表

座舱联动：

- 灯光：日常 → 柔和专注
- 音乐：开启 → 关闭
- 座椅：标准 → 端正坐姿
- 温度：24℃ → 23℃
- 提醒方式：普通 → 轻声提醒

到达前提醒：

- 简历
- 身份证
- 作品集
- 仪表整理
- 下车点和入楼指引

### 6.2 机场准备

用户语音：

> 我要去首都国际机场，帮我看下要注意什么

AI 识别：

- 出行目的：机场
- 用户状态：时间敏感
- 核心诉求：时间确认 + 证件行李检查 + 到达流程
- 推荐模式：机场准备模式

AI 计划：

1. 确认到达时间和航站楼
2. 检查身份证、护照、行李、充电宝
3. 提前说明下车后的行动顺序

座舱联动：

- ETA 和 checklist 置顶
- 音乐音量降低
- 提醒方式更明确
- 温度略微调低
- 座椅保持标准

到达前提醒：

- 航站楼
- 身份证或护照
- 登机信息
- 随身行李
- 下车后入口指引

### 6.3 商务会议准备

用户语音：

> 我等会儿要参加客户会议，帮我整理一下状态

AI 识别：

- 出行目的：客户会议
- 用户状态：需要专注
- 核心诉求：会议目标 + 表达重点 + 资料提醒
- 推荐模式：商务会议准备模式

AI 计划：

1. 回顾会议目标
2. 整理 3 个关键发言点
3. 到达前检查资料和客户称呼

座舱联动：

- 灯光：日常 → 冷静专注
- 音乐：开启 → 关闭
- 座椅：标准 → 办公坐姿
- 温度：24℃ → 23℃
- 提醒方式：普通 → 简洁提示

到达前提醒：

- 电脑
- 会议资料
- 客户称呼
- 三个表达重点
- 到达入口

### 6.4 休息恢复

用户语音：

> 我有点累，想在车上休息一下

AI 识别：

- 出行目的：休息恢复
- 用户状态：疲惫
- 核心诉求：低打扰 + 到达前唤醒
- 推荐模式：休息恢复模式

AI 计划：

1. 调暗灯光并降低声音
2. 保持低打扰休息
3. 到达前 5 分钟温和唤醒并提醒物品

座舱联动：

- 灯光：日常 → 暗光
- 音乐：关闭或低音量白噪音
- 座椅：标准 → 放松
- 温度：24℃ → 25℃
- 提醒方式：普通 → 轻柔唤醒

到达前提醒：

- 温和唤醒
- 手机
- 包
- 随身物品
- 准备下车

## 7. 推荐代码结构

如果项目采用标准 Vite + React 结构，建议如下：

```text
src/
  App.jsx
  main.jsx
  styles/
    global.css
  data/
    scenarios.js
  components/
    TripStatusBar.jsx
    AgentInteractionPanel.jsx
    DecisionPlanPanel.jsx
    CabinStatePanel.jsx
    VoiceCommandBar.jsx
```

也可以根据实际工程调整，但必须保持以下原则：

- mock 数据与 UI 组件分离
- 场景内容由 data 驱动
- 4 个场景复用同一套主流程组件
- 不为每个场景复制一整套页面
- 状态变化集中管理，避免散落在多个组件中难以维护

## 8. 状态管理建议

本项目不需要引入复杂全局状态库。

第一版 Demo 只保留必要状态：

- `selectedScenarioId`
- `currentStep`
- `confirmationChoice`
- `showExceptionRisk`
- `messages`
- `demoStarted`

推荐 `currentStep`：

```js
[
  'boarding',
  'agent_intro',
  'asking_purpose',
  'voice_input',
  'understanding',
  'intent_result',
  'plan_confirm',
  'executing',
  'arrival_reminder'
]
```

其他内容尽量从 `selectedScenarioId`、`currentStep`、`confirmationChoice` 派生：

- `intentResult` 从 `selectedScenario.intent` 派生
- `activePlan` 根据 `confirmationChoice` 选择 `plan` 或 `adjustedPlan`
- `cabinState` 根据 `currentStep` 判断展示 `cabinBefore` 或 `cabinAfter`
- `agentStatus` 根据 `currentStep` 映射
- `arrivalReminder` 从 `selectedScenario.arrivalReminder` 派生

## 9. 代码规范

### 9.1 JavaScript 与 React

- 使用函数组件
- 使用清晰的 props 命名
- 组件职责单一
- 避免过深组件嵌套
- 避免把大段 mock data 写在组件内部
- 避免重复硬编码 4 个场景流程
- 不引入不必要的第三方库
- 不实现超出 MVP 范围的功能

### 9.2 CSS

- 使用普通 CSS 即可
- 样式命名应语义清晰
- 优先保证大屏可读性
- 使用大字号、大间距、大卡片
- 避免密集表格和小字说明
- 避免移动端优先导致大屏视觉稀疏或比例失衡
- 需要保证核心信息在常见桌面大屏宽度下无需滚动即可理解

### 9.3 Mock Data

Mock data 应集中维护，例如 `src/data/scenarios.js`。

每个场景建议包含：

- `id`
- `title`
- `voiceText`
- `destination`
- `eta`
- `intent`
- `plan`
- `adjustedPlan`
- `cabinBefore`
- `cabinAfter`
- `checklist`
- `aiReplies`
- `arrivalReminder`
- `exceptionFallback`

### 9.4 文案规范

AI 文案应：

- 简短
- 主动
- 冷静
- 有帮助
- 尊重用户确认权

避免：

- 长篇解释
- 过度拟人
- 情绪陪伴式话术
- “我什么都能帮你”的泛助手表达
- 暗示真实 API、真实车控或真实发送已经执行

### 9.5 安全边界表达

涉及以下操作时必须明确是 mock：

- 通知发送
- 路线调整
- 下车点变更
- 车控执行
- 航班/高铁/日历信息读取

建议文案：

> 本 Demo 仅展示 mock 效果，不会真实发送消息、变更路线或控制车辆。

## 10. 验收标准

### 10.1 页面观感

- 一眼能看出是 Robotaxi 后排大屏界面
- 字号足够大，远距离可读
- 卡片足够大，信息密度低
- 有科技感，但不花哨
- 不像普通 Chatbot
- 不像后台 dashboard
- 不像车控设置页

### 10.2 主流程

必须完整跑通：

1. 默认上车状态
2. AI 主动欢迎
3. 行程与安全状态展示
4. AI 能力介绍
5. AI 主动询问
6. 用户点击语音指令
7. 展示语音转文字
8. 展示 AI 识别结果
9. 展示 AI 准备计划
10. 展示三个确认分支
11. 开启计划后展示座舱联动
12. 到达前展示 checklist 和下车提醒

### 10.3 四个核心场景

以下场景都必须可点击并跑通主流程：

- 面试准备
- 机场准备
- 商务会议准备
- 休息恢复

每个场景必须展示：

- 用户语音
- AI 识别结果
- 到达前准备计划
- 用户确认方式
- 座舱联动变化
- 到达前收口提醒

### 10.4 确认分支

必须实现：

- 开启计划：进入主路径
- 调整计划：展示 mock 调整后的轻量计划
- 暂不需要：进入低打扰待命状态

### 10.5 异常兜底

面试场景必须展示一次 mock 迟到风险兜底：

- ETA 延误
- AI 解释原因
- AI 给出可选方案
- 生成 mock 通知文案
- 提示真实发送或路线变更需要二次确认
- 不执行真实发送或路线变更

### 10.6 技术边界

必须确认：

- 无后端依赖
- 无数据库依赖
- 无真实 ASR/TTS
- 无真实地图 API
- 无真实车控 API
- 无真实消息发送
- 所有数据来自前端 mock

## 11. 开发与验证要求

在提交开发结果前，应尽量完成：

- 启动本地 Vite dev server
- 确认页面可以正常打开
- 手动测试 4 个场景
- 手动测试 3 个确认分支
- 手动测试面试场景迟到风险兜底
- 检查大屏视口下的信息可读性
- 检查页面是否有明显溢出、遮挡、文字过小、布局拥挤问题

如项目中存在 lint/build/test 命令，应运行对应命令。

如果无法运行，需要在最终说明中解释原因。

## 12. 禁止事项

开发过程中不得主动加入以下内容：

- 新主场景
- 开放式自由聊天
- 城市探索模式
- 支付、订票、改签、预约等交易能力
- 真实登录系统
- 用户画像系统
- 后台管理系统
- 数据看板
- 多乘客复杂账户体系
- 与 MVP 无关的复杂动画
- 与 ArriveMate 产品定位无关的功能入口

任何新增功能都必须服务于：

> 帮助乘客在 Robotaxi 到达前完成准备。

## 13. 后续迭代方向

以下内容仅作为未来方向，本期不实现：

- 真实 ASR/TTS
- 实时地图 ETA 和拥堵检测
- 真实座舱车控
- 日历数据读取
- 航班/高铁数据读取
- 用户偏好记忆
- 用户授权后的消息发送
- 路线或下车点建议
- 数据埋点，包括场景选择率、计划开启率、调整率、取消率、提醒完成率
- 更完整的安全确认机制
